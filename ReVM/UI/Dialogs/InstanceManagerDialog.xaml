<Window x:Class="ReVM.InstanceManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instances" Height="560" Width="520"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display"
        WindowStartupLocation="CenterOwner">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="26" ResizeBorderThickness="4" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="BgMain" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BgPanel" Color="#252526"/>
        <SolidColorBrush x:Key="BgTitle" Color="#323233"/>
        <SolidColorBrush x:Key="BgHover" Color="#3E3E3E"/>
        <SolidColorBrush x:Key="BgActive" Color="#37373D"/>
        <SolidColorBrush x:Key="BgPressed" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#858585"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3E3E3E"/>
        <SolidColorBrush x:Key="Danger" Color="#E81123"/>

        <Style x:Key="ChromeBtn" TargetType="Button">
            <Setter Property="Width" Value="34"/><Setter Property="Height" Value="26"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="Bd" Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="CloseChromeBtn" TargetType="Button" BasedOn="{StaticResource ChromeBtn}"><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource Danger}"/></Trigger></Style.Triggers></Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource TextSecondary}"/><Setter Property="FontSize" Value="10"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
        <Style x:Key="LdTextBox" TargetType="TextBox"><Setter Property="Height" Value="28"/><Setter Property="Background" Value="#1E1E1E"/><Setter Property="Foreground" Value="{StaticResource TextPrimary}"/><Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/><Setter Property="Padding" Value="8,5"/><Setter Property="FontSize" Value="12"/></Style>
        <Style x:Key="DialogButton" TargetType="Button"><Setter Property="Height" Value="28"/><Setter Property="MinWidth" Value="72"/><Setter Property="Padding" Value="12,0"/><Setter Property="Background" Value="#2D2D2D"/><Setter Property="Foreground" Value="{StaticResource TextPrimary}"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FontSize" Value="12"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="Bd" Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource BgPressed}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}"><Setter Property="Background" Value="{StaticResource BgActive}"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
        <Style x:Key="ComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="Bd" Property="Background" Value="#3E3E3E"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="Bd" Property="Background" Value="#37373D"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboToggle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LdCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="26"/>
            <Setter Property="MinHeight" Value="26"/>
            <Setter Property="Background" Value="#29292E"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ComboItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid Height="{TemplateBinding Height}" SnapsToDevicePixels="True">
                            <ToggleButton x:Name="Toggle" Style="{StaticResource ComboToggle}" Height="{TemplateBinding Height}" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                            <Border x:Name="Bd" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" IsHitTestVisible="False">
                                <Grid>
                                    <TextBlock Text="{Binding SelectedItem.Content, RelativeSource={RelativeSource TemplatedParent}}"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontSize="{TemplateBinding FontSize}"
                                               Margin="10,3,30,3"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <Path Data="M4,6 L8,10 L12,6" Stroke="#858585" StrokeThickness="1.15" Fill="Transparent" Width="10" Height="7" Stretch="Uniform" HorizontalAlignment="Right" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PopupAnimation="Fade">
                                <Border Background="#252526" BorderBrush="#454545" BorderThickness="1" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Hidden">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="#5A5A5A"/></Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="#858585"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SliderRepeat" TargetType="RepeatButton"><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></Setter.Value></Setter></Style>
        <Style x:Key="SliderThumb" TargetType="Thumb"><Setter Property="Width" Value="12"/><Setter Property="Height" Value="12"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Thumb"><Border x:Name="ThumbBd" Width="12" Height="12" Background="#CCCCCC" BorderBrush="#1E1E1E" BorderThickness="1"/><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThumbBd" Property="Background" Value="#FFFFFF"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="ThumbBd" Property="Background" Value="#FFFFFF"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
        <Style TargetType="Slider"><Setter Property="Height" Value="22"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Slider"><Grid Height="22"><Border Height="3" Background="#252526" BorderBrush="#3E3E3E" BorderThickness="1" VerticalAlignment="Center"/><Track x:Name="PART_Track"><Track.DecreaseRepeatButton><RepeatButton Style="{StaticResource SliderRepeat}" Command="Slider.DecreaseLarge"/></Track.DecreaseRepeatButton><Track.Thumb><Thumb Style="{StaticResource SliderThumb}"/></Track.Thumb><Track.IncreaseRepeatButton><RepeatButton Style="{StaticResource SliderRepeat}" Command="Slider.IncreaseLarge"/></Track.IncreaseRepeatButton></Track></Grid></ControlTemplate></Setter.Value></Setter></Style>
        <Style x:Key="VsScrollBar" TargetType="{x:Type ScrollBar}"><Setter Property="Background" Value="#1E1E1E"/><Setter Property="Foreground" Value="#5A5A5A"/><Setter Property="BorderBrush" Value="#2D2D2D"/><Setter Property="SnapsToDevicePixels" Value="True"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type ScrollBar}"><Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0"><Track x:Name="PART_Track" IsDirectionReversed="True"><Track.Thumb><Thumb Background="{TemplateBinding Foreground}"><Thumb.Template><ControlTemplate TargetType="{x:Type Thumb}"><Border x:Name="ThumbBd" Background="{TemplateBinding Background}" CornerRadius="4" Margin="2"/><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThumbBd" Property="Background" Value="#707070"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="ThumbBd" Property="Background" Value="#8A8A8A"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Thumb.Template></Thumb></Track.Thumb></Track></Border></ControlTemplate></Setter.Value></Setter></Style>
        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource VsScrollBar}"/>

        <DataTemplate x:Key="InstanceRowTemplate">
            <Border Background="#252526" BorderBrush="#333333" BorderThickness="0,0,0,1" Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" Foreground="#D4D4D4" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Foreground="#858585" FontFamily="Cascadia Mono, Consolas" FontSize="10" Margin="0,2,0,0"><Run Text="{Binding BootProfile}"/><Run Text="  ·  "/><Run Text="{Binding CpuCount}"/><Run Text="c / "/><Run Text="{Binding RamMB}"/><Run Text="m / "/><Run Text="{Binding StorageGB}"/><Run Text="g storage"/></TextBlock>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Status}" Foreground="#A6A6A6" FontSize="10" FontFamily="Cascadia Mono, Consolas" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Button Content="{Binding RunActionText}" Style="{StaticResource DialogButton}" Click="ToggleInstance_Click" Tag="{Binding Id}" Margin="0,0,6,0"/>
                        <Button Content="Edit" Style="{StaticResource DialogButton}" Click="EditInstance_Click" Tag="{Binding Id}" Margin="0,0,6,0"/>
                        <Button Content="Delete" Style="{StaticResource DialogButton}" Click="DeleteInstance_Click" Tag="{Binding Id}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border Background="{StaticResource BgMain}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="26"/><RowDefinition Height="*"/><RowDefinition Height="206"/></Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource BgTitle}" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="Instances" Foreground="{StaticResource TextPrimary}" FontSize="11" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button Grid.Column="1" Style="{StaticResource CloseChromeBtn}" Click="Close_Click" WindowChrome.IsHitTestVisibleInChrome="True"><Path Data="M2,2 L12,12 M12,2 L2,12" Stroke="#CCCCCC" StrokeThickness="1" Width="10" Height="10" Stretch="Uniform"/></Button>
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0">
                <ScrollViewer.Resources><Style TargetType="ScrollBar" BasedOn="{StaticResource VsScrollBar}"/></ScrollViewer.Resources>
                <ItemsControl x:Name="InstanceList" ItemTemplate="{StaticResource InstanceRowTemplate}"/>
            </ScrollViewer>
            <Border Grid.Row="2" Background="{StaticResource BgPanel}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0" Padding="14,12">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="160"/></Grid.ColumnDefinitions><TextBox x:Name="NameBox" Text="Android" Style="{StaticResource LdTextBox}"/><ComboBox x:Name="ProfileCombo" Grid.Column="1" Style="{StaticResource LdCombo}" Margin="8,0,0,0" SelectedIndex="0"><ComboBoxItem Content="Release" Tag="api34-persona"/><ComboBoxItem Content="Resizable" Tag="api34-resizable"/><ComboBoxItem Content="Play" Tag="play-compat"/></ComboBox></Grid>
                    <Grid Grid.Row="1" Margin="0,0,0,5"><Grid.ColumnDefinitions><ColumnDefinition Width="64"/><ColumnDefinition Width="*"/><ColumnDefinition Width="74"/></Grid.ColumnDefinitions><TextBlock Text="CPU" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/><Slider x:Name="CpuSlider" Grid.Column="1" Minimum="1" Maximum="64" Value="2" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="CpuSlider_ValueChanged"/><TextBox x:Name="CpuBox" Grid.Column="2" Text="2" Style="{StaticResource LdTextBox}" Height="24" FontFamily="Cascadia Mono, Consolas" FontSize="10" TextAlignment="Right" LostFocus="CpuBox_LostFocus" KeyDown="NumericBox_KeyDown"/></Grid>
                    <Grid Grid.Row="2" Margin="0,0,0,5"><Grid.ColumnDefinitions><ColumnDefinition Width="64"/><ColumnDefinition Width="*"/><ColumnDefinition Width="74"/></Grid.ColumnDefinitions><TextBlock Text="RAM MB" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/><Slider x:Name="RamSlider" Grid.Column="1" Minimum="1024" Maximum="524288" Value="2048" TickFrequency="1024" IsSnapToTickEnabled="True" ValueChanged="RamSlider_ValueChanged"/><TextBox x:Name="RamBox" Grid.Column="2" Text="2048" Style="{StaticResource LdTextBox}" Height="24" FontFamily="Cascadia Mono, Consolas" FontSize="10" TextAlignment="Right" LostFocus="RamBox_LostFocus" KeyDown="NumericBox_KeyDown"/></Grid>
                    <Grid Grid.Row="3"><Grid.ColumnDefinitions><ColumnDefinition Width="64"/><ColumnDefinition Width="*"/><ColumnDefinition Width="74"/><ColumnDefinition Width="80"/></Grid.ColumnDefinitions><TextBlock Text="Storage GB" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/><Slider x:Name="StorageSlider" Grid.Column="1" Minimum="8" Maximum="4096" Value="8" TickFrequency="8" IsSnapToTickEnabled="True" ValueChanged="StorageSlider_ValueChanged"/><TextBox x:Name="StorageBox" Grid.Column="2" Text="8" Style="{StaticResource LdTextBox}" Height="24" FontFamily="Cascadia Mono, Consolas" FontSize="10" TextAlignment="Right" LostFocus="StorageBox_LostFocus" KeyDown="NumericBox_KeyDown"/><Button x:Name="SaveInstanceButton" Grid.Column="3" Content="Add" Style="{StaticResource PrimaryButton}" Click="AddInstance_Click" Margin="8,0,0,0"/></Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>